jwt:
  secret: ${JWT.SECRET}
spring:

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include:
          - prometheus
          - health
          - info
  endpoint:
    health:
      show-details: always