jwt:
  secret: ${JWT.SECRET}
app:
  version: ${{APP_VERSION:unknown}}

server:
  port: 8080

management:
  endpoints:
    web:
      exposure:
        include:
          - prometheus
          - health
          - info
  endpoint:
    health:
      show-details: always