
spring:
  datasource:
    url: jdbc:postgresql://postgresql:5432/postgres?profileSQL=true
    username: postgres
    password: localpwd
#    hikari:
#      maximum-pool-size: 100

  jpa:
    properties:
      hibernate:
        format_sql: true
#        use_sql_comments: true
    hibernate:
      ddl-auto: validate
    generate-ddl: false
    open-in-view: false
#    show-sql: true

  flyway:
    locations: classpath:flyway
    baseline-on-migrate: true
    baseline-version: 1
    enabled: true
server:
  port: 8080
