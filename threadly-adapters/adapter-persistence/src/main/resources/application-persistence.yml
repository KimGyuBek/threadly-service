spring:


  jpa:
    properties:
      hibernate:
        format_sql: true
#        use_sql_comments: true
    hibernate:
      ddl-auto: validate
    generate-ddl: false
    open-in-view: false
#    show-sql: true

  flyway:
    locations: classpath:flyway
    baseline-on-migrate: true
    baseline-version: 1
    enabled: true
    schemas: batch
